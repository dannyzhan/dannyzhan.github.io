comments:
  allowedFields: ["name", "email", "url", "message", "replying_to"]
  allowedOrigins: ["9932.js.org"]
  requiredFields: ["name", "message"]
  branch: "master"
  commitMessage: "add comment by <Staticman>\n\npost-{options.page_guid}#comment-{@id}"
  filename: "comment-{@timestamp}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp"
  moderation: false
  name: "Danny Zhan's Blog"
  path: "_data/comments/{options.page_guid}"  
  transforms:
    email: md5
